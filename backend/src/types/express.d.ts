/**
 * Global Express type augmentations.
 * Additional per-feature augmentations (e.g. AuthenticatedRequest) live in
 * their own modules to keep interfaces small and focused (ISP).
 */

// Re-export nothing — this file exists to house future global augmentations.
export {};
